<nav class="navbar navbar-expand-lg navbar-bg">
	<div class="container-fluid">
	<a class="navbar-brand" href="#">Calorie Counter</a>
	<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>
	<div class="collapse navbar-collapse flex-grow-1 text-right" id="navbarSupportedContent">
		<ul class="navbar-nav ms-auto flex-nowrap">
		<li class="nav-item">
			<a class="nav-link m-2 menu-item <% if(typeof home !== 'undefined') { %>
                active
            <% } %>
            " href="/site">Home</a>
		</li>
		<li class="nav-item">
			<a class="nav-link m-2 menu-item <% if(typeof about !== 'undefined') { %>
                active
            <% } %>
            " href="/">Sobre nós</a>
		</li>
		<% if(typeof isOff === 'undefined') { %>
			<li class="nav-item">
				<a class="nav-link m-2 menu-item <% if(typeof login !== 'undefined') { %>
						active
					<% } %>
					" aria-current="page" href="/site/login">Login</a>
			</li>
		<% } else { %>
			<li class="nav-item">
				<a class="nav-link m-2 menu-item <% if(typeof perfil !== 'undefined') { %>
						active
					<% } %>
					" aria-current="page" href="/site/perfil">Perfil</a>
			</li>
			<li class="nav-item">
				<a class="nav-link m-2 menu-item <% if(typeof pontos !== 'undefined') { %>
						active
					<% } %>
					" aria-current="page" href="/site/pontos">Pontos</a>
			</li>

			
			<li class="nav-item">
				<a class="nav-link m-2 menu-item <% if(typeof login !== 'undefined') { %>
						active
					<% } %>
					" aria-current="page" href="/" id="logout">Logout</a>
			</li>
		<% } %>
		</ul>
	</div>
	</div>
</nav>